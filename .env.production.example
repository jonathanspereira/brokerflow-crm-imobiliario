# ==================================
# BrokerFlow CRM - Variáveis Raiz (Docker Compose)
# ==================================
# Este arquivo define variáveis usadas no docker-compose.yml da raiz
# Para backend e frontend, veja os arquivos .env.production.example específicos

# ========================================
# Database (PostgreSQL)
# ========================================
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_strong_password
POSTGRES_DB=your_db_name
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}

# ========================================
# Backend API
# ========================================
NODE_ENV=production
PORT=3001

# JWT & Encryption (CRÍTICO - GERE NOVOS VALORES)
JWT_SECRET=your_jwt_secret_min_32_chars
ENCRYPTION_KEY=your_encryption_key_64_hex_chars

# CORS
CORS_ORIGIN=https://your-domain.com

# WhatsApp
WHATSAPP_DATA_PATH=/app/whatsapp_data
WHATSAPP_HEADLESS=true
WHATSAPP_QR_TIMEOUT=60000

# ========================================
# Frontend
# ========================================
NEXT_PUBLIC_API_URL=https://api.your-domain.com/api/v1
REDIS_URL=redis://redis:6379

# ========================================
# Observabilidade (Opcional)
# ========================================
SENTRY_DSN=
OTEL_ENABLED=false

# ==================================
# 🔧 Comandos para Gerar Secrets
# ==================================

# JWT_SECRET (base64, ~32 chars):
#   openssl rand -base64 32
# ENCRYPTION_KEY (hex 64 chars / 32 bytes):
#   openssl rand -hex 32
# POSTGRES_PASSWORD (random 32 chars):
#   openssl rand -base64 32 | tr -d "=+/" | cut -c1-32

# ==================================
# 📝 Instruções de Uso
# ==================================

# 1. Copie este arquivo para .env.production
# 2. Substitua TODOS os valores de exemplo por valores reais e seguros
# 3. NUNCA commite o arquivo .env.production (já está no .gitignore)
# 4. Use com: docker compose -f docker-compose.yml -f docker-compose.prod.yml --env-file .env.production up
