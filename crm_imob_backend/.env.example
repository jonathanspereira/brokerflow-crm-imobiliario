# Server
PORT=3001
NODE_ENV=development

# Database
DATABASE_URL="postgresql://crm_user:230328@localhost:5432/crm_imob"

# WhatsApp
WHATSAPP_HEADLESS=true
WHATSAPP_DATA_PATH=./whatsapp_data
WHATSAPP_QR_TIMEOUT=60000

# JWT
# CRÍTICO: Gerar com: openssl rand -base64 32
# Mínimo 32 caracteres em produção
JWT_SECRET=your_secret_key_here_CHANGE_IN_PRODUCTION_MIN_32_CHARS
JWT_EXPIRATION=7d

# CORS
# Múltiplos origins separados por vírgula
# Exemplo: http://localhost:3000,https://app.example.com,https://admin.example.com
CORS_ORIGIN=http://localhost:3000
