# ==================================
# BrokerFlow CRM - Variáveis de Produção
# ==================================

# CRÍTICAS (obrigatórias)
# ------------------------
NODE_ENV=production
DATABASE_URL=postgresql://your_db_user:your_strong_password@your_db_host:5432/your_db_name
JWT_SECRET=your_jwt_secret_min_32_chars
ENCRYPTION_KEY=your_encryption_key_64_hex_chars

# IMPORTANTES (recomendadas)
# ---------------------------
PORT=3001
CORS_ORIGIN=https://your-domain.com
WHATSAPP_DATA_PATH=/app/whatsapp_data
WHATSAPP_HEADLESS=true
WHATSAPP_QR_TIMEOUT=60000
REDIS_URL=redis://redis:6379

# REFRESH TOKENS (opcionais - valores padrão)
# --------------------------------------------
REFRESH_TOKEN_TTL_DAYS=7
ACCESS_TOKEN_TTL_MINUTES=60

# RATE LIMITING (opcionais - valores padrão)
# -------------------------------------------
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=200

# OBSERVABILIDADE (opcionais)
# ----------------------------
SENTRY_DSN=

# ==================================
# Instruções de Configuração
# ==================================

# 1. JWT_SECRET:
#    - Gere com: openssl rand -base64 32
#    - Mínimo 32 caracteres
#    - NUNCA use o valor padrão de desenvolvimento
# 2. ENCRYPTION_KEY:
#    - Gere com: openssl rand -hex 32
#    - Exatamente 64 caracteres hexadecimais (32 bytes)
# 3. DATABASE_URL:
#    - Formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# 4. CORS_ORIGIN:
#    - URL completa do frontend em produção
#    - Múltiplos origins separados por vírgula
# 5. SENTRY_DSN (opcional):
#    - Obtenha em https://sentry.io após criar projeto
#    - Error tracking em produção
